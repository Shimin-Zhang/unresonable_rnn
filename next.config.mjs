/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export can be added later with generateStaticParams
  // For now, using standard Next.js rendering

  images: {
    unoptimized: true,
  },
}

export default nextConfig
